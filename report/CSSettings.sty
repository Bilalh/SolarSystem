\ProvidesPackage{CSSettings}
\bibliographystyle{IEEEtran}

\usepackage{pdfpages} %inculde pdfs
% Listing (fold)
\usepackage{listings} 
\usepackage{textcomp} %for upquotes 
                           
\lstset{
	tabsize=4,
	mathescape=true, 
	escapechar=§,
	upquote=true,
	basicstyle=\ttfamily,
} %(end)

% Pictures (fold)
\usepackage[pdftex]{graphicx}
\usepackage{subfigure}
\usepackage{wrapfig} % (end)

% Geometry (fold)
\usepackage[pdftex]{graphicx}
\usepackage{geometry}
% \geometry{bottom=2.5cm, top = 2.5cm}
\renewcommand{\topfraction}{0.97}	% max fraction of floats at top
\renewcommand{\bottomfraction}{0.97}	% max fraction of floats at bottom
\renewcommand{\textfraction}{0.01}

% Very small margins
\geometry{left=1.5cm, right=1.5cm, bottom=2.4cm, top = 2.4cm}
% (end)

% Maths (fold)
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{ wasysym } %Bowtie for na join (end)             

% Other Packages (fold)
% Makes small lists
\usepackage{enumitem}
\usepackage[utf8]{inputenc}
%(end)

% Not on Packages/Settings (fold)
%small sections
 % \usepackage[small,compact]{titlesec}
% Small text nicer
 % \usepackage{microtype}

% \usepackage{alltt}
% \renewcommand{\ttdefault}{txtt}

% \usepackage[bookmarks=true]{hyperref}
% \setcounter{secnumdepth}{5}
% \setcounter{tocdepth}{5}

% (end)

% shortcuts (fold)
\def\studentID{080008164}
\def\fullName{Bilal Hussain}
\newcommand{\picwidth}{6.3in}

\def\*{\ensuremath{\times}}
\def\<={\ensuremath{\leqslant}}
\def\>={\ensuremath{\geqslant}}
\def\={\ensuremath{\neq}}
\def\~{\ensuremath{\approx}}

\def\s{\textsuperscript{* }}
\def\ts{\textsuperscript}
\def\-{\subscript}
\def\_{\textunderscore}
\def\set#1{\{ #1 \}}
\def\t#1{\text{#1}}

\def\abs#1{\mathopen| #1 \mathclose|}
\def\Integer{\mathsf{Z\hspace{-0.4em}Z}}
\def\Natural{\mathrm{I\!N}}
\def\Real{\mathrm{I\!R}}
%(end)

%p code	(fold)
\def\And{\ensuremath{\textbf{ and }}}
\def\Not{\ensuremath{\textbf{ not }}}
\def\Xor{\ensuremath{\textbf{ xor }}}
\def\Or{\ensuremath{\textbf{ or }}}

\def\If{\ensuremath{\textbf{if }}}	
\def\For{\ensuremath{\textbf{for }}}	
\def\While{\ensuremath{\textbf{while }}}
\def\Else{\ensuremath{\textbf{else }}}
\def\Do{\ensuremath{\textbf{ do}}}
\def\To{\ensuremath{\textbf{ to }}}
\def\Then{\ensuremath{\textbf{ then}}}
\def\Until{\ensuremath{\textbf{ until }}}
\def\End{\ensuremath{\textbf{end}}}
\def\Downto{\ensuremath{\textbf{ downto }}}

\def\Swap{\ensuremath{\textbf{swap }}}
%(end)

% Metadata
\newcommand{\theTitle}{}
\title{\theTitle}
\author{\studentID}

\renewcommand{\topfraction}{0.97}	% max fraction of floats at top
\renewcommand{\bottomfraction}{0.97}	% max fraction of floats at bottom
\renewcommand{\textfraction}{0.01}


%	default Headers
% \pagestyle{headings} 

% fancy headers (fold)
\usepackage{fancyhdr} 
\pagestyle{fancy}
\fancyhf{} % removes all headers

\usepackage{ifthen}
\newcommand{\currentSection}{
\ifthenelse{\equal{\rightmark}{}}{\leftmark}{\rightmark}
}

\fancyhead[LO,RE]{\slshape \nouppercase{\currentSection}} 
\fancyhead[LE,RO]{\thepage} 
\fancyhead[CEO]{\hspace{10pt} \studentID  \hspace{5pt}-- \theTitle}
%(end)

%graph dot (fold)
\newcommand{\graphc}[2][]{ 
  \immediate\write18{bash -c "dot -Tpdf  figures/dot/#2.dot -o  figures/#2.pdf"}	 
}



\lstnewenvironment{sql}[1][] % colored sql using \begin {sql} (fold)
	{
		\lstset{
		tabsize=4,
		mathescape=true, 
		escapechar=§,
		upquote=true,
		language=sql, 
		basicstyle=\small\ttfamily,
		% keywords
		keywordstyle=[1]\color{blue},
		keywords=[1]{
						select, from, where, as, join, on, group, order, by, 
						desc, asc, set, or, and, like, between, and, union, 
						all, having, limit, inner, cross,join, distinct,
						left, right, full, update, insert, into, values, delete,
						%
						delimiter,create, function, returns, begin, case,
						when, then, if, else, end, return, declare, loop,
						view, 
						%
						alter, table, create, drop, add, check, constraint
					},
		% functions
		keywordstyle=[2]\color{Periwinkle},
		keywords=[2]{
						count, concat, sum, avg, max,min, concatenate, convert, 
						lower, substring, translate, trim, upper, round, ifnull,
						now, date_format, group_concat, char_length
					},
		% types
		keywordstyle=[3]\color{RoyalBlue},
		keywords=[3]{
						bigint, bigserial, bit, boolean, box, bytea, cidr, circle,
						date, double, precision, inet, int, integer, line,
						macaddr, money, oid, path, point, polygon, real, serial,
						smallint, sysdate, text, datetime, date, varchar
					},
		%
		showstringspaces=false,
		stringstyle=\color{OliveGreen}\ttfamily,
		#1	
		}
	}
	{}
%(end)
